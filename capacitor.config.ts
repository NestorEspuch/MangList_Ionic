import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'MangList',
  appName: 'MangList',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
